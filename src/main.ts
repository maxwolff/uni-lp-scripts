/* 
Main:
    act = new Account()
    while true: mainLoop(account)

MainLoop(account):
    for row in db:
        obj = build Obj 
        tx[] | delete = LPposition.step()
        if delete: 
            arr.delete(position)
            db.delete(position)
        elif tx:
            await account.executeTx(tx[])
    for delta in watchedFile: 
        if create:
            obj = new LPPosition(JSON)
            arr.push(obj)
            dbFile.push(JSON)
        if delete: 
            obj = lookupObj(arr)
            obj.unwind() 

/post(lpConfig)
    save in db

*/
